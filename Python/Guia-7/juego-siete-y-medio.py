import random
# GUIA 7 - EJERCICIO 4 (programas interactivos con input)
# 3. Vamos a escribir un programa para simular el juego conocido como 7 y medio. El mismo deberá generar un número
#    aleatorio entre 0 y 12 (excluyendo el 8 y 9) y deberá luego preguntarle al usuario si desea seguir sacando otra
#   “carta” o plantarse. En este último caso el programa debe terminar. Los números aleatorios obtenidos deberán sumarse 
#   según el número obtenido salvo por las “figuras” (10, 11 y 12) que sumarán medio punto cada una.
#   El programa debe ir acumulando los valores y si se pasa de 7.5 debe informar que el usuario ha perdido. 
#   Al finalizar la función devuelve el historial de “cartas” que hizo que el usuario gane o pierda.

# Para generar números pseudo-aleatorios entre 1 y 12 utilizaremos la función random.randint(1,12). 
# Al mismo tiempo, la función random.choice() puede ser de gran ayuda a la hora de repartir cartas.

def siete_y_medio():
    pass # después lo hago ...